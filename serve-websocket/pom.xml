<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>build.serve</groupId>
        <artifactId>serve-project</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>serve-websocket</artifactId>

    <name>Serve WebSocket</name>
    <description>RFC 6455 WebSocket support with frame codec and upgrade handling.</description>

    <dependencies>
        <dependency>
            <groupId>build.serve</groupId>
            <artifactId>serve-foundation</artifactId>
        </dependency>

        <!-- Test Dependencies -->
        <dependency>
            <groupId>build.serve</groupId>
            <artifactId>serve-testing</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>--add-opens jdk.httpserver/sun.net.httpserver=build.serve.websocket
                    </argLine>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
