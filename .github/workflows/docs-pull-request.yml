name: Docs Pull Request

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'serve.build-site/**'

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Deno
        uses: denoland/setup-deno@v2

      - name: Check
        working-directory: serve.build-site
        run: deno task ok

      - name: Build
        working-directory: serve.build-site
        run: deno task build
